import eventos from eventos.js

function filtrar(filtro, eventos) {
    const filter = filtro;
    const events = eventos;

    const array_size = size(eventos);

    var result = [array_size]

    for(var i = 0; i < array_size; i++) {
        const keys = eventos[i].value

    }

    var result = events.array.forEach(element => {
        
    });


}

function mostrar_filtros() {

}